import DropdownMenu from './DropdownMenu';
import { renderWithTheme } from 'utils/test-utils';

describe('Component: DropdownMenu', () => {
  it('should render correctly', () => {
    renderWithTheme(<DropdownMenu />);
  });
});
